import { Box, Heading, SimpleGrid, Text, Flex, Container, Avatar, HStack, useBoolean, Divider } from "@chakra-ui/react";

function Profile() {
    return (
        <HStack
            cursor='pointer'
        >
            <Text fontSize='xl' fontWeight='bold'>UNDERWOOD</Text>
        </HStack>
    );
}

export default Profile;